"""
Django settings for Soil_Moisture project.

Generated by 'django-admin startproject' using Django 3.2.

For more information on this file, see
https://docs.djangoproject.com/en/3.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/3.2/ref/settings/
"""

from pathlib import Path
import pymysql
import os

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Install MySQL client library
pymysql.install_as_MySQLdb()


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/3.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-7%x!puz0aszc0b3bvvv*si338g*nyz19fyzmxtlbnuqgob%gh('

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'accounts',
    'jazzmin',
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'Soil_Moisture.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / "templates"],  # or os.path.join(BASE_DIR, "templates")
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'Soil_Moisture.wsgi.application'


# Database
# https://docs.djangoproject.com/en/3.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'soil_moisture',
        'USER': 'django_user',
        'PASSWORD': '@Renitah2023',
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            #'init_command': "SET sql_mode='STRICT_TRANS_TABLES', innodb_strict_mode=1",  
            'charset': 'utf8',
        },
    }
}


# Password validation
# https://docs.djangoproject.com/en/3.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]

AUTH_USER_MODEL = 'accounts.CustomUser'


# Internationalization
# https://docs.djangoproject.com/en/3.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/3.2/howto/static-files/

STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'

# Default primary key field type
# https://docs.djangoproject.com/en/3.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

from django.db.backends.signals import connection_created
from django.dispatch import receiver

@receiver(connection_created)
def set_strict_mode(sender, connection, **kwargs):
    with connection.cursor() as cursor:
        cursor.execute("SET SESSION sql_mode='STRICT_TRANS_TABLES'")

EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'omwesigyeseezi5@gmail.com'
EMAIL_HOST_PASSWORD = 'qybu rdif wphj gkco'
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER

TWILIO_ACCOUNT_SID = 'your_account_sid'
TWILIO_AUTH_TOKEN = 'your_auth_token'
TWILIO_PHONE_NUMBER = '+256 777721569'  # Your Twilio phone number

JAZZMIN_SETTINGS = {
    "site_title": "Soil Moisture Admin",
    "site_header": "Soil Moisture Detector Admin",
    "site_brand": "SoilMoisture",
    "welcome_sign": "Welcome to the Soil Moisture Detector Admin Dashboard",
    "copyright": "SoilMoisture Project",
    "search_model": ["accounts.CustomUser", "accounts.PredictionRecord"],
    "user_avatar": None,

    # Top menu links
    "topmenu_links": [
        {"name": "Dashboard", "url": "admin:index", "permissions": ["auth.view_user"]},
        {"name": "Analytics", "url": "/admin/accounts/predictionrecord/", "permissions": ["accounts.view_predictionrecord"]},
        {"name": "Soil Models", "url": "/admin/accounts/mlmodel/", "permissions": ["accounts.view_mlmodel"]},
        {"model": "accounts.customuser"},
    ],

    # Custom icons for apps/models
    "icons": {
        "accounts.CustomUser": "fas fa-user",
        "accounts.PredictionRecord": "fas fa-chart-line",
        "accounts.MLModel": "fas fa-robot",
    },

    # Custom links in the user menu
    "usermenu_links": [
        {
            "name": "Profile",
            "url": "/admin/accounts/customuser/{user.id}/change/",
            "icon": "fas fa-user-cog"
        },
    ],
    "show_usermenu_profile": False,

    # Custom dashboard cards (Jazzmin doesn't support widgets out of the box, but you can use custom templates)
    "custom_links": {
        "accounts": [
            {
                "name": "Download All Predictions",
                "url": "/admin/accounts/predictionrecord/?download=all",
                "icon": "fas fa-download",
                "permissions": ["accounts.view_predictionrecord"]
            }
        ]
    },

    # Change the default theme
    "theme": "minty",
}

MEDIA_ROOT = BASE_DIR / 'media'